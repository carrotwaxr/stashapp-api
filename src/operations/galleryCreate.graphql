mutation galleryCreate($input: GalleryCreateInput!) {
  galleryCreate(input: $input) {
    id
    title
    details
    date
    rating100
    studio {
      id
      name
    }
    performers {
      id
      name
    }
    tags {
      id
      name
    }
  }
}
